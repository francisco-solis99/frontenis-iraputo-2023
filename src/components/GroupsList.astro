---
import Group from './Group.astro';
const { groups } = Astro.props;
---

<section class="grid grid-cols-[repeat(auto-fit,minmax(280px,1fr))] gap-8">
  {
    groups.map(groupInfo => {
      return (
        <Group name={groupInfo.name} teams={groupInfo.teams}/>
      );
    })
  }
</section>
