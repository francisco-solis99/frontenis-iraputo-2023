
<button id="btn-add-to-calendar" class="border-0 bg-transparent">
  <slot/>
</button>

<script type="module">
  import { atcb_action as addToCalendar } from '../../node_modules/add-to-calendar-button/dist/module/index.js'
  const config = {
    name: 'Torneo Frontenis Irapuato 2023',
    description: 'Primer torneo de las fresas de frontenis en Irapuato',
    startDate: '2023-03-26',
    startTime: '08:00',
    endTime: '17:45',
    timeZone: 'America/Mexico_City',
    location: 'Irapuato Guanajuato México',
    listStyle: 'modal',
    options: ['Google', 'Apple', 'Outlook.com', 'Yahoo']
  }

  const button = document.getElementById('btn-add-to-calendar')
  if (button) {
    button.addEventListener('click', () => addToCalendar(config, button))
  }
</script>
